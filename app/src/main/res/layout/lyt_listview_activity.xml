<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#1e000000"
    android:weightSum="1">

    <EditText
        android:layout_width="360dp"
        android:layout_height="wrap_content"
        android:id="@+id/studyTitleET"
        android:layout_gravity="center_horizontal"
        android:hint="@string/study_title_hint"
        android:imeOptions="actionDone"
        android:singleLine="true"
        android:textStyle="bold"
        android:textSize="24sp"
        android:background="@drawable/edit_text_style"
        android:layout_marginTop="20dp"
        android:paddingTop="10dp"
        android:paddingBottom="10dp"
        android:paddingLeft="10dp"
        android:layout_centerHorizontal="true" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/enter_top_reasons"
        android:textAppearance="?android:attr/textAppearanceLarge"
        android:id="@+id/studyTitleTV"
        android:layout_gravity="center_horizontal"
        android:layout_below="@+id/studyTitleET"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="20dp" />

    <ListView
        android:id="@+id/listViewMain"
        android:layout_width="match_parent"
        android:layout_height="247dp"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_below="@+id/studyTitleTV"
        android:layout_above="@+id/startButton"
        android:layout_marginTop="10dp"
        android:layout_weight="0.50">
    </ListView>

    <Button
        android:layout_width="175dp"
        android:layout_height="50dp"
        android:text="@string/start_study"
        android:id="@+id/startButton"
        android:layout_gravity="bottom|center_horizontal"
        android:onClick="startDowntime"
        android:layout_centerHorizontal="true"
        android:layout_alignParentBottom="true"
        android:textStyle="bold"
        android:textSize="20sp"
        android:layout_marginBottom="15dp"
        android:background="#FFFFFF" />

</RelativeLayout>